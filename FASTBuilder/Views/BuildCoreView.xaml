<UserControl x:Class="FastBuilder.Views.BuildCoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FastBuilder.Views"
             mc:Ignorable="d" 
             d:DesignHeight="48" d:DesignWidth="300">

    <ItemsControl x:Name="Jobs"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Center"
                  Width="{Binding UIJobsTotalWidth}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas Height="28" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="{x:Type ContentPresenter}">
                <Setter Property="MinWidth" Value="10" />
                <Setter Property="Width" Value="{Binding UIWidth}" />
                <Setter Property="Canvas.Left" Value="{Binding UILeft}" />
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>

</UserControl>
